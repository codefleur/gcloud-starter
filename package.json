{
  "name": "codemaster-gcloud-stopper-basic",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "debug": "export $(cat .env | sed 's/#.*//g' | xargs) && node index.js",
    "build": "sudo docker build -t choephixpi/gcloud ."
  },
  "author": "cx",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/compute": "^1.1.5",
    "express": "^4.18.2"
  }
}
